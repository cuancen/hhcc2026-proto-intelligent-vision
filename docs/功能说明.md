# Eva 智能座舱 · 功能说明手册

> 给团队的功能速查手册：每个功能**是什么、怎么触发、背后做了什么**。
> 路演 Q&A 前建议全员通读一遍；所有阈值数字集中在文末参数速查表。

- 启动：`npm run dev` → http://localhost:5173
- 一句话定位：**机器视觉守护的 L2 辅助驾驶座舱智能体**——浏览器本地人脸识别监测驾驶员疲劳/分神，与行车工况双通道融合，让 Eva 智能体"看得见驾驶员"地主动干预。
- 安全边界：全项目定位 **L2 辅助驾驶**（自适应巡航 + 车道居中），驾驶员始终是责任主体；Eva 的所有动作只到"提醒、建议、座舱调节、辅助降级"为止。

---

## 一、界面导览（打开页面后看到什么）

```
┌────────────────────── TopBar 顶栏：车速/里程/L2状态/Eva模式 · 字号缩放 · 高对比 ──────────────────────┐
│ 左栏                          │ 中栏                            │ 右栏                              │
│ · EvaAgent 智能体对话         │ · SceneView 态势主视图(Canvas)  │ · AdasPanel  辅助驾驶控制         │
│ · DriverPanel 驾驶员状态      │ · DataflowBar 数据流指示         │ · CabinPanel 座舱环境            │
│                              │ · DmsPanel 视觉监测(摄像头)      │ · AlertCenter 告警中心           │
│                              │                                │ · StatsPanel  统计面板           │
├────────────────────── ControlBar 底栏：场景切换 · 事件注入 · 疲劳滑杆 · 自动演示 · 速率 ──────────────┤
```

---

## 二、核心功能详解

### F1 机器视觉 DMS（驾驶员监测）——项目最大亮点

**是什么**：打开摄像头后，在浏览器本地识别人脸 **478 个关键点**，实时算出驾驶员的疲劳与分神指标。画面全程本机推理，**不上传任何影像**。

**怎么触发**：DMS 面板点「📷 开启摄像头监测」→ 浏览器授权摄像头。没有摄像头时点「模拟信号」，走**同一套指标算法**，演示永不中断。

**背后做了什么**（三层）：

1. **模型推理**：MediaPipe FaceLandmarker（自托管于 `public/models/`，3.7MB，Apache-2.0），WASM/GPU 本地运行；模型加载三源容灾（本地 → Google CDN），GPU 失败自动降 CPU。
2. **指标解算**（`src/vision/metrics.ts`，纯函数、可单测）：
   - **EAR 眼睛纵横比**：每眼 6 个关键点，`(上下眼睑距离之和) / (2×眼宽)`；EAR < 0.15 判定为闭眼帧；
   - **PERCLOS**：30 秒滑动窗口内闭眼帧占比（学术界公认的疲劳金标准指标）；
   - **眨眼频率**：闭眼事件计数折算每分钟次数；
   - **头部姿态**：从模型输出的 4×4 变换矩阵解出 yaw/pitch 欧拉角，偏航 >22° 或俯仰 >18° 记为"视线离开"，持续累计秒数。
3. **融合决策**：视觉指标折算成"视觉疲劳度"（PERCLOS × 185；视线离开每秒 +6、封顶 40），与工况仿真疲劳度**取强融合**（谁大听谁的）→ 得到综合疲劳度 0–100。

### F2 疲劳守护（双阈值分级干预）

**是什么**：综合疲劳度越过阈值时，Eva 分两级干预，而不是一味唠叨。

| 级别 | 触发 | Eva 的动作 |
|---|---|---|
| 温柔关怀 | 疲劳度 ≥ **60** | 加强通风一档、调低温度 1.5℃、切轻音乐，语言关怀提醒休息（8 仿真分钟冷却） |
| 紧急干预 | 疲劳度 ≥ **85** | 紧急告警 + 弹出**用户选择分支**：「立即休息 / 再坚持一会儿」（15 仿真分钟冷却） |

**休息分支逻辑**：选「立即休息」→ 进入休息模式（关音乐、开按摩、氛围灯调暖、23.5℃），疲劳恢复到低位自动退出；选「再坚持」→ **6 仿真分钟后若仍是重度疲劳会再次升级提醒**（尊重用户但不放弃守护）。

**怎么演示**：底栏「疲劳注入」滑杆直接拉到 62 / 88，或等疲劳场景自然累积。

### F3 分神守护 + L2 降级/恢复

**是什么**：驾驶员视线持续离开路面时，系统不只是提醒，还会**动真格降级辅助驾驶**。

| 视线离开时长 | 系统动作 |
|---|---|
| ≥ 2 秒 | warn 告警"请注意路面"（1.5 仿真分钟冷却） |
| ≥ 4 秒 | **urgent 告警 + L2 立即降级**：目标车速 ×0.85、拉大跟车时距，Eva 语音催促回看路面 |
| 回正 < 0.5 秒 | 自动恢复正常 L2 策略，发 info 通知 |

另外：视觉开着但**画面里没有人**（驾驶员离开座位/趴下）→ 每 10 仿真分钟 warn 提醒确认坐姿。

### F4 Eva 拟人智能体

**是什么**：有五种工作模式的对话式智能体，左栏对话流 + 情境快捷回复气泡。

**模式（优先级从高到低）**：干预中（L2 已降级）→ 休息引导中 → 谨慎模式（复杂路况）→ 守护中（疲劳≥60）→ 观察中。

**情绪四联动**：情绪值 ≤32（低落）→ 暖色氛围灯 + 舒缓音乐 + 轻柔按摩；≥68（亢奋）→ 切舒缓曲 + 加强通风——都是"帮驾驶员回到平稳状态"。

### F5 自然语言指令（对话输入框直接打字）

| 你说 | Eva 做 |
|---|---|
| 「困/累/瞌睡/犯乏」 | 疲劳设为 ≥63 + 通风提神组合（风量+1、降温、动感音乐）+ 主动建议服务区 |
| 「热」/「冷」 | 降/升温 1.5℃（区间 18–30℃） |
| 「音乐/歌/来点」 | 轻音乐→动感→新闻 循环切换 |
| 「静音/关掉音乐」 | 关闭音乐 |
| 「按摩」 | 开关座椅按摩 |
| 「导航/多久/还有多远」 | 报剩余公里数 + 预计分钟 |
| 「休息/服务区/停车」 | 进入休息模式 |
| 「开启/关闭辅助（L2）」 | 开关 L2 辅助驾驶 |
| 其他 | 礼貌说明能力范围 |

### F6 复杂路况协同（舱驾联动）

**是什么**：雨、夜、拥堵三个因子**计数 ≥2** 时自动进入「谨慎模式」：屏蔽娱乐（音乐关闭并记住之前曲目）、强化安全播报、修正目标车速（雨 ×0.85、夜 ×0.92、弯道最多 ×0.94，连乘）。路况缓解后**自动恢复**娱乐与常规座舱服务。

### F7 态势主视图（Canvas 伪 3D 道路）

中栏的行车画面：昼夜切换、雨天、弯道左右摆动、前车刹车灯、HUD 叠加（车速/L2 状态/降级提示）。下雨时自动"开雨刷"，入夜自动切夜间主题——与仿真状态实时联动。下方 DataflowBar 以动画展示「感知 → Eva 决策 → 控制」数据流向。

### F8 三大演示场景 + 60 秒自动演示

| 场景 | 剧情脚本 |
|---|---|
| ☀ 日常通勤 | 上车问候 + 人脸识别确认身份 → 按习惯调座舱（22.5℃/青碧氛围/轻音乐）→ 规划 12.6km 常用路线 |
| 😮‍💨 疲劳守护 | 高速 96km、L2 开启、疲劳加速累积 → 完整走一遍 60 关怀 / 85 干预 / 休息分支 |
| ⛈ 复杂路况 | 脚本化依次注入：雨 → 拥堵 → 夜 → 前车急刹 → 触发谨慎模式与舱驾协同 |

**自动演示**（底栏 ▶ 或快捷键 D）：约 60 秒按剧本走完三场景（速率自动 ×2、无视觉时自动开模拟信号），每步有字幕提示；随时可停，**停下不清理现场**，评委可接着手动探索。手动注入事件：底栏「☔ 雨 / 🌙 夜 / ⚠ 前车急刹」按钮 + 疲劳滑杆。

### F9 无障碍

字号四档缩放（偏好持久化到本地）、高对比模式、全键盘操作、跳转链、紧急告警 aria-live assertive 播报、respect prefers-reduced-motion。

### F10 仿真系统（所有动态行为的底层）

- 车速：目标速度一阶惯性趋近（时间常数 0.9 仿真分钟）；拥堵工况叠加正弦"走走停停"；前车急刹目标 −22 km/h；
- 疲劳累积：基础 0.08/仿真分钟，拥堵 ×2.75，高速 >85km/h ×5.25，疲劳场景再 ×1.6；休息时以 2.6/分钟恢复；
- 速率三档 ×½ / ×1 / ×2，底栏一键切换。

---

## 三、快捷键速查

| 键 | 作用 |
|---|---|
| `1` / `2` / `3` | 切换 日常通勤 / 疲劳守护 / 复杂路况 |
| `D` | 启动/停止自动演示 |
| `L` | 开/关 L2 辅助驾驶 |

（在输入框打字时不会误触发）

## 四、关键参数速查（Q&A 可能问到的数字）

| 参数 | 值 | 出处 |
|---|---|---|
| 人脸关键点数 | 478 点 | MediaPipe FaceLandmarker |
| 闭眼判定 EAR | < 0.15 | `vision/metrics.ts` |
| PERCLOS 窗口 | 30 秒滑动窗口 | 同上 |
| 视线离开阈值 | 偏航 >22°、俯仰 >18° | 同上 |
| 分神预警 / L2 降级 | 2 秒 / 4 秒 | `core/params.ts` |
| 疲劳关怀 / 紧急阈值 | 60 / 85（0–100） | 同上 |
| 拒绝休息再提醒 | 6 仿真分钟后 | 同上 |
| 复杂路况触发 | 雨/夜/拥堵因子 ≥2 | 同上 |
| PERCLOS→疲劳折算 | ×185（0.35 → 65，恰越关怀线） | `core/sim.ts` |
| 回归测试 | 25 项全过（内核 + 视觉指标） | `tests/` |

## 五、一图讲清数据流（路演可画）

```
摄像头 → MediaPipe 478点(本地推理) ──EAR/PERCLOS/头姿──┐
                                                        ├→ 双通道融合疲劳度 → Eva 规则引擎(冷却闸门+场景脚本)
工况仿真(车速/路况/时长) ──仿真疲劳度──────────────────┘          │
                                                    ┌────────────┼────────────┐
                                                  对话/告警      座舱调节      L2 降级/恢复
```

> 工程亮点（技术复杂度维度可讲）：内核**零 DOM**（可独立测试/迁移）、参数唯一出处、视觉多源容灾 + 模拟信号回退、25 项回归测试、全部浏览器端完成（无服务器依赖，断网可跑）。

---

## 六、追加：F11 路演讲解横幅 DemoBanner（08-22 凌晨追加）

**是什么**：自动演示运行时，页面顶部悬浮一张讲解卡，逐步告诉评委「第几步 / 正在看什么 / 这一段想表达什么」，底部有进度点和停止按钮。解决的问题是：评委第一次看演示时不知道该关注哪里。

**怎么触发**：底栏「▶ 自动演示」或快捷键 `D`——横幅随演示自动出现/消失。

**九步讲解剧本**（与演示事件逐拍对齐）：

| 步 | 讲解要点 |
|---|---|
| 1 | 日常通勤：人脸识别问候 + 按习惯无感调节座舱（主动服务） |
| 2 | 疲劳守护：眨眼/PERCLOS/头姿与工况双通道监测 |
| 3 | 轻度疲劳 62 → 60 关怀线：通风/降温/轻音乐（分级干预） |
| 4 | 重度疲劳 88 → 85 紧急线：告警 + 休息选择分支 |
| 5 | 选「立即休息」：座舱一键接管；拒绝则 6 分钟后再提醒 |
| 6 | 复杂路况：雨+夜+拥堵 ≥2 → 屏蔽娱乐、舱驾协同 |
| 7 | 路况缓解 → 自动恢复（有边界的智能） |
| 8 | 语音指令：自然语言理解能力清单 |
| 9 | 结束语：引导评委亲手开摄像头体验真实机器视觉 |

**背后**：剧本是纯数据（`DEMO_STEPS`：时间严格递增 + 每步必有讲解文案，有 3 项回归测试保护）；横幅 `role="status" aria-live="polite"`，读屏用户同样能听到讲解；`prefers-reduced-motion` 下入场动画自动关闭。
